server:
  port: 8810

eureka:
  client:
    # 指定服务注册中心的位置
    service-url:
      defaultZone: http://localhost:8710/eureka/

# 显示服务IP地址
eureka:
  instance:
    prefer-ip-address: true





config:
  file:
    name: scis-order-service-dev.yaml