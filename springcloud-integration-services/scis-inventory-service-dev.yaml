server:
  port: 8820

eureka:
  # 显示服务IP地址
  instance:
      prefer-ip-address: true
  client:
    # 指定服务注册中心的位置
    service-url:
      defaultZone: http://localhost:8710/eureka/





config:
  file:
    name: scis-inventory-service-dev.yaml